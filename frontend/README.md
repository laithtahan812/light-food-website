# ЛАЙТ FOOD — Frontend (React + Vite)

## Быстрый старт
1) Установите Node.js 18+  
2) Скопируйте `.env.example` в `.env` (при необходимости измените URL API)

```bash
cd frontend
npm install
npm run dev
```

Откройте: `http://localhost:5173`
